[package]
name = "start"
version = "0.1.0"
edition = "2021"
description = "Brings up the dev environment container and opens code-server in a native window"

[build-dependencies]
tauri-build = "2"

[dependencies]
tauri = { version = "2", features = [] }
ureq = "2"
